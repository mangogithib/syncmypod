# LF everywhere, on every platform.
#
# Developed on Windows and run on Windows, macOS and Linux. CRLF creeping into a
# shell script or a Dockerfile fails at runtime with an unhelpful error rather
# than at commit time.
* text=auto eol=lf

*.png binary
*.jpg binary
*.ico binary
